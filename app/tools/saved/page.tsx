"use client";
import {useEffect,useState} from "react";
import {Trash2,FolderOpen,ArrowRight} from "lucide-react";
import {deleteSavedProject,loadSavedProjects,SavedProject} from "@/lib/saved-projects";
export default function SavedProjectsPage(){const [items,setItems]=useState<SavedProject[]>([]);useEffect(()=>setItems(loadSavedProjects()),[]);const remove=(id:string)=>{deleteSavedProject(id);setItems(loadSavedProjects())};return <main className="min-h-screen bg-[#080808] px-4 py-10 text-white"><div className="mx-auto max-w-5xl"><a href="/" className="mb-8 inline-flex items-center gap-2 text-white/50 hover:text-white"><ArrowRight size={18}/>بازگشت</a><div className="mb-8"><div className="text-xs font-bold tracking-[4px] text-[#d4a72c]">TRAXTOR MEDIA</div><h1 className="mt-2 text-3xl font-black">طرح‌های ذخیره‌شده</h1><p className="mt-2 text-white/40">طرح‌های خود را نگه دارید و بعداً برای ویرایش یا خروجی دوباره باز کنید.</p></div>{items.length===0?<div className="rounded-3xl border border-white/10 bg-white/[.03] p-12 text-center text-white/40">هنوز طرحی ذخیره نشده است.</div>:<div className="grid gap-4 md:grid-cols-2">{items.map(p=><article key={p.id} className="rounded-2xl border border-white/10 bg-white/[.03] p-5"><div className="flex items-start justify-between gap-4"><div><h2 className="font-black text-lg">{p.name}</h2><div className="mt-2 text-sm text-white/40">{p.tool}</div><div className="mt-1 text-xs text-white/25">آخرین ویرایش: {new Date(p.updatedAt).toLocaleString("fa-IR")}</div></div><FolderOpen className="text-[#d4a72c]"/></div><div className="mt-5 flex justify-end"><button onClick={()=>remove(p.id)} className="flex items-center gap-2 rounded-xl bg-red-500/10 px-4 py-2 text-sm text-red-300"><Trash2 size={16}/>حذف</button></div></article>)}</div>}</div></main>}
