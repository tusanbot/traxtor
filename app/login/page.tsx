"use client";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
export default function LoginPage(){const router=useRouter();const [username,setUsername]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({username,password})});if(r.ok)router.push("/tools");else{setError("نام کاربری یا رمز عبور نادرست است.");setBusy(false)}}return <main dir="rtl" className="flex min-h-screen items-center justify-center bg-[#070707] px-4 text-white"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-white/10 bg-[#111] p-7 shadow-2xl"><div className="mb-7"><div className="text-xs font-bold tracking-[4px] text-[#d4a72c]">TRAXTOR MEDIA</div><h1 className="mt-2 text-3xl font-black">ورود به ابزارها</h1><p className="mt-2 text-sm text-white/45">برای ساخت و ذخیره طرح وارد شوید.</p></div><div className="space-y-3"><input autoComplete="username" value={username} onChange={e=>setUsername(e.target.value)} placeholder="نام کاربری" className="w-full rounded-xl bg-black/40 px-4 py-3 outline-none"/><input autoComplete="current-password" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="رمز عبور" className="w-full rounded-xl bg-black/40 px-4 py-3 outline-none"/></div>{error&&<div className="mt-3 rounded-xl bg-red-500/10 px-3 py-2 text-sm text-red-300">{error}</div>}<button disabled={busy} className="mt-5 w-full rounded-xl bg-[#d71920] py-3 font-bold disabled:opacity-50">{busy?"در حال ورود…":"ورود"}</button></form></main>}
